@model IEnumerable<FirstProject.Models.Attendance>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Checkin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Checkout)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Emp)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Checkin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Checkout)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Emp.Empid)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Attenid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Attenid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Attenid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
